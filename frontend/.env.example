VITE_SUPABASE_URL=https://vdeuqmtixtnmyipbdlfc.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here
# Local dev: use relative API paths + root script `npm run dev` (starts backend :8787 and Vite; Vite proxies /api).
# Set only for a remote API, e.g. VITE_BACKEND_URL=https://api.yoursite.com
# VITE_BACKEND_URL=
# Prefer relative Zoom base in dev so it uses the same proxy:
VITE_ZOOM_API_BASE=/api/zoom
